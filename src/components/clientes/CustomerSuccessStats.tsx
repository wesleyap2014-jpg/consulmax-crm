import React from"react";import{WorkItem}from"./customerSuccessModel";
export default function Stats({items}:{items:WorkItem[]}){const n=(s:string)=>items.filter(x=>x.cs.status===s).length;return <div className="grid grid-cols-3 gap-3 xl:grid-cols-6">{[["Pendentes",n("pendente")],["Em validação",n("em_validacao")],["Validados",n("validado")],["Atenção",n("atencao")],["Divergências",n("divergencia")],["Críticos",n("critico")]].map(([l,v])=><div key={String(l)} className="rounded-2xl border p-3"><div className="text-xs font-bold text-slate-500">{l}</div><div className="text-2xl font-black">{v}</div></div>)}</div>}
