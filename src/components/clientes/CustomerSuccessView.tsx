import React,{useMemo,useState}from"react";import{CsStatus}from"./customerSuccessModel";import Stats from"./CustomerSuccessStats";import Toolbar from"./CustomerSuccessToolbar";import Table from"./CustomerSuccessTable";import Modal from"./CustomerSuccessModal";
export default function View({cs}:{cs:any}){const[q,setQ]=useState(""),[f,setF]=useState<CsStatus|"todos">("todos"),rows=useMemo(()=>cs.items.filter((i:any)=>(f==="todos"||i.cs.status===f)&&[i.cliente?.nome,i.lead?.nome,i.venda.grupo,i.venda.cota,i.vendedor_nome].join(" ").toLowerCase().includes(q.toLowerCase())),[cs.items,q,f]);return <div className="space-y-4"><Stats items={cs.items}/><div className="space-y-3 rounded-2xl border bg-white p-4"><Toolbar q={q} setQ={setQ} f={f} setF={setF} load={cs.load}/><Table items={rows} loading={cs.loading} open={cs.open}/></div>{cs.active&&cs.form&&<Modal item={cs.active} f={cs.form} set={cs.setForm} saving={cs.saving} userName={cs.userName} onAttempt={cs.attempt} onSave={cs.save} onReview={cs.review} onClose={cs.close}/>}</div>}
