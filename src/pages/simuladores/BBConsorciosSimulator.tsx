// src/pages/simuladores/BBConsorciosSimulator.tsx
import React, { useEffect, useMemo, useRef, useState } from "react";
import { supabase } from "@/lib/supabaseClient";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { AlertTriangle, Bike, Building2, Calculator, Car, CheckCircle2, Copy, Download, Home, Loader2, PackageCheck, Save, ShieldCheck, Truck, UserRound } from "lucide-react";

type SegmentoBB = "auto_ipca" | "auto_fipe" | "pesados" | "imoveis" | "motocicletas" | "outros_bens";
type LanceKey = "livre" | "primeiro_fixo" | "segundo_fixo" | "limitado" | "fidelidade";
type RegraPos = "saldo_devedor_prazo_restante" | "mantem_parcela_reduz_prazo";
type AnyRow = Record<string, any>;
type CreditRange = { id: string; label: string; valor: number };
type PrazoRule = { id: string; prazo: number; taxaAdmPct: number; fundoReservaPct: number };
type LanceOption = { key: LanceKey; enabled: boolean; nomeComercial: string; pct: number };
type BBConfig = { creditRanges: CreditRange[]; prazoRules: PrazoRule[]; lanceOptions: LanceOption[]; maxLanceEmbutidoPct: number; regraPosContemplacao: RegraPos; observacoesRegra?: string; assemblyResult?: AnyRow | null };
type BBGroup = AnyRow & { id: string; grupo: string; segmento: string; nome_grupo?: string | null; observacoes?: string | null; config?: BBConfig | AnyRow | string | null; is_active?: boolean | null };
type Lead = { id: string; nome: string; telefone: string | null; email: string | null; owner_id: string | null };
type LoggedUserProfile = { id: string; nome?: string | null; name?: string | null; phone?: string | null; telefone?: string | null; role?: string | null; user_role?: string | null };

const C = { ruby: "#A11C27", navy: "#1E293F", gold: "#B5A573", lightGold: "#E0CE8C", red: "#EC111A" };
const segmentos = [
  { key: "auto_ipca" as const, label: "Auto IPCA", statusLabel: "Automóvel", icon: Car },
  { key: "auto_fipe" as const, label: "Auto FIPE", statusLabel: "Automóvel", icon: ShieldCheck },
  { key: "pesados" as const, label: "Pesados", statusLabel: "Pesados", icon: Truck },
  { key: "imoveis" as const, label: "Imóveis", statusLabel: "Imóvel", icon: Home },
  { key: "motocicletas" as const, label: "Motocicleta", statusLabel: "Moto", icon: Bike },
  { key: "outros_bens" as const, label: "Outros Bens", statusLabel: "Serviços e bens", icon: PackageCheck },
];
const LANCE_LABELS: Record<LanceKey, string> = { livre: "Lance Livre", primeiro_fixo: "1º Lance Fixo", segundo_fixo: "2º Lance Fixo", limitado: "Lance Limitado", fidelidade: "Lance Fidelidade" };
const DEFAULT_LANCES: LanceOption[] = [
  { key: "livre", enabled: true, nomeComercial: "Lance Livre", pct: 0 },
  { key: "primeiro_fixo", enabled: false, nomeComercial: "1º Lance Fixo", pct: 0 },
  { key: "segundo_fixo", enabled: false, nomeComercial: "2º Lance Fixo", pct: 0 },
  { key: "limitado", enabled: false, nomeComercial: "Lance Limitado", pct: 0 },
  { key: "fidelidade", enabled: false, nomeComercial: "Lance Fidelidade", pct: 0 },
];
const MEDIAN_KEYS = ["medianaPct", "mediana_pct_contemplado", "mediana_pct_lance_livre", "mediana_lance_livre", "mediana_lance", "median_lance", "media_lance_livre", "media_lance", "avg_lance", "lance_medio", "median", "mediana", "medianaLance", "medianaLancePct", "mediaLance", "mediaLancePct", "lanceMedio", "lanceMedioPct", "averageBidPct", "avgBidPct", "medianBidPct"];
const MIN_KEYS = ["menorPct", "menor_pct_contemplado", "menor_pct_lance_livre", "menor_lance_livre", "menor_lance_pct", "lance_minimo_pct", "lowest_bid_pct", "menor_lance", "lowestBidPct", "menorLancePct", "minBidPct", "min_bid_pct", "lowestBid"];
const MAX_KEYS = ["maiorPct", "maior_pct_contemplado", "maior_pct_lance_livre", "maior_lance_livre", "maior_lance_pct", "lance_maximo_pct", "highest_bid_pct", "maior_lance", "highestBidPct", "maiorLancePct", "maxBidPct", "max_bid_pct", "highestBid"];
const BID_KEYS = ["percentual_lance", "lance_pct", "lance_percentual", "percentual", "pct", "bidPct", "bid_percent", "valor_lance_percentual", "lancePct", "lancePercentual", "percentualLance", "bid"];

function brMoney(v: number) { return (Number(v) || 0).toLocaleString("pt-BR", { style: "currency", currency: "BRL", maximumFractionDigits: 2 }); }
function toNumber(value: any) { if (typeof value === "number") return Number.isFinite(value) ? value : 0; if (value === null || value === undefined) return 0; const raw = String(value).trim(); if (!raw) return 0; const cleaned = raw.replace(/R\$/gi, "").replace(/%/g, "").replace(/\s/g, "").replace(/[^0-9,.-]/g, ""); const n = Number(cleaned.includes(",") ? cleaned.replace(/\./g, "").replace(",", ".") : cleaned); return Number.isFinite(n) ? n : 0; }
function pctToDecimal(value: any) { const n = toNumber(value); if (!n) return 0; return n > 1 ? n / 100 : n; }
function pctToPercent(value: any) { const n = toNumber(value); if (!n) return 0; return n <= 1 ? n * 100 : n; }
function parseMoney(value: string) { const digits = (value || "").replace(/\D/g, ""); return digits ? Number(digits) / 100 : 0; }
function formatMoneyInput(value: number) { return brMoney(value || 0); }
function parsePercent(value: string) { const n = toNumber(value); return n / 100; }
function formatPercentInput(decimal?: number | null) { return ((Number(decimal || 0) * 100).toFixed(4)).replace(".", ","); }
function pctHuman(decimal?: number | null, digits = 2) { return `${(Number(decimal || 0) * 100).toFixed(digits).replace(".", ",")}%`; }
function pctTextFromPercent(percent?: number | null, digits = 1) { return percent && percent > 0 ? `${percent.toFixed(digits).replace(".", ",")}%` : "Livre"; }
function numberFrom(value: string) { const n = Number(String(value || "").replace(/\D/g, "")); return Number.isFinite(n) ? n : 0; }
function onlyDigits(value?: string | null) { return String(value || "").replace(/\D/g, ""); }
function clamp(n: number, min: number, max: number) { return Math.max(min, Math.min(max, n)); }
function rawConfig(group?: BBGroup | null): AnyRow { const cfg = group?.config; if (typeof cfg === "string") { try { return JSON.parse(cfg); } catch { return {}; } } return (cfg || {}) as AnyRow; }
function assemblyRow(group?: BBGroup | null): AnyRow { const cfg = rawConfig(group); const assembly = cfg.assemblyResult || cfg.resultadoAssembleia || cfg.assembly || cfg.assemblies || cfg.resultados; return assembly && !Array.isArray(assembly) && typeof assembly === "object" ? assembly : {}; }
function pickPercent(row: AnyRow | null | undefined, keys: string[]) { if (!row) return 0; for (const key of keys) { const pct = pctToPercent(row[key]); if (pct > 0) return pct; } return 0; }
function asRows(value: unknown): AnyRow[] { if (!value) return []; if (Array.isArray(value)) return value.filter((v) => v && typeof v === "object") as AnyRow[]; if (typeof value !== "object") return []; const row = value as AnyRow; const nested = row.items || row.rows || row.data || row.results || row.resultados || row.contemplados || row.lances || row.assemblyRows || row.assembly_rows || row.creditRanges; if (Array.isArray(nested)) return nested.filter((v) => v && typeof v === "object") as AnyRow[]; return [row]; }
function nestedRows(value: unknown): AnyRow[] { const direct = asRows(value); const nested: AnyRow[] = []; direct.forEach((row) => { nested.push(row); Object.values(row).forEach((item) => { if (Array.isArray(item)) nested.push(...asRows(item)); else if (item && typeof item === "object") nested.push(...asRows(item)); }); }); return nested; }
function median(values: number[]) { const nums = values.filter((v) => Number.isFinite(v) && v > 0).sort((a, b) => a - b); if (!nums.length) return 0; const mid = Math.floor(nums.length / 2); return nums.length % 2 ? nums[mid] : (nums[mid - 1] + nums[mid]) / 2; }

function normalizeSegmento(segmento?: string | null): SegmentoBB | string { const value = String(segmento || "").trim().toLowerCase(); if (["motocicleta", "motocicletas", "moto", "motos"].includes(value)) return "motocicletas"; return value || "auto_ipca"; }
function getSegmentoLabel(segmento?: string | null) { const normalized = normalizeSegmento(segmento); return segmentos.find((s) => s.key === normalized)?.label || segmento || "—"; }
function getStatusSegmentoLabel(segmento?: string | null) { const normalized = normalizeSegmento(segmento); return segmentos.find((s) => s.key === normalized)?.statusLabel || getSegmentoLabel(segmento); }
function brDate(value?: string | null) { if (!value) return "A definir"; const raw = String(value).trim(); const match = raw.match(/(\d{2}\/\d{2}\/\d{4})/); if (match) return match[1]; const iso = raw.match(/^(\d{4})-(\d{2})-(\d{2})/); if (iso) return `${iso[3]}/${iso[2]}/${iso[1]}`; const parsed = new Date(raw); return Number.isNaN(parsed.getTime()) ? raw : parsed.toLocaleDateString("pt-BR"); }
function getUserPhone(user?: LoggedUserProfile | null) { return onlyDigits(user?.phone || user?.telefone || ""); }
function buildWhatsappLink(user?: LoggedUserProfile | null) { const phone = getUserPhone(user); return phone ? `https://wa.me/${phone.startsWith("55") ? phone : `55${phone}`}` : ""; }

function normalizeConfig(group?: BBGroup | null): BBConfig {
  const raw = rawConfig(group);
  const ranges: CreditRange[] = Array.isArray(raw.creditRanges) && raw.creditRanges.length
    ? raw.creditRanges.map((r: any, i: number) => { const valor = toNumber(r?.valor ?? r?.credito ?? r?.value ?? r); return { id: String(r?.id || r?.key || `cred-${i}`), label: String(r?.label || r?.nome || r?.name || brMoney(valor)), valor }; })
    : [
        { id: "legacy-min", label: brMoney(toNumber(group?.credito_min)), valor: toNumber(group?.credito_min) },
        { id: "legacy-max", label: brMoney(toNumber(group?.credito_max)), valor: toNumber(group?.credito_max) },
      ];
  const prazoRules: PrazoRule[] = Array.isArray(raw.prazoRules) && raw.prazoRules.length
    ? raw.prazoRules.map((r: any, i: number) => ({ id: String(r.id || `prazo-${i}`), prazo: toNumber(r.prazo || r.prazo_meses || r.months || r.term || 1), taxaAdmPct: pctToDecimal(r.taxaAdmPct ?? r.taxa_adm_pct ?? group?.taxa_adm_pct), fundoReservaPct: pctToDecimal(r.fundoReservaPct ?? r.fundo_reserva_pct ?? group?.fundo_reserva_pct) }))
    : [{ id: "legacy-prazo", prazo: toNumber(group?.prazo_max || group?.prazo_min || 80), taxaAdmPct: pctToDecimal(group?.taxa_adm_pct), fundoReservaPct: pctToDecimal(group?.fundo_reserva_pct) }];
  const byKey = new Map<LanceKey, LanceOption>();
  DEFAULT_LANCES.forEach((l) => byKey.set(l.key, { ...l }));
  if (Array.isArray(raw.lanceOptions)) raw.lanceOptions.forEach((l: any) => { if (l?.key && byKey.has(l.key)) byKey.set(l.key, { key: l.key, enabled: l.enabled !== false, nomeComercial: String(l.nomeComercial || LANCE_LABELS[l.key as LanceKey]), pct: pctToDecimal(l.pct) }); });
  else {
    byKey.set("livre", { key: "livre", enabled: group?.permite_lance_livre !== false, nomeComercial: "Lance Livre", pct: 0 });
    byKey.set("primeiro_fixo", { key: "primeiro_fixo", enabled: group?.permite_fixo_25 !== false, nomeComercial: "Fixo 25%", pct: 0.25 });
    byKey.set("segundo_fixo", { key: "segundo_fixo", enabled: group?.permite_fixo_50 !== false, nomeComercial: "Fixo 50%", pct: 0.5 });
  }
  return { creditRanges: ranges.filter((r, i, arr) => r.valor > 0 && arr.findIndex((x) => x.valor === r.valor) === i).sort((a, b) => a.valor - b.valor), prazoRules: prazoRules.filter((r) => r.prazo > 0).sort((a, b) => a.prazo - b.prazo), lanceOptions: Array.from(byKey.values()), maxLanceEmbutidoPct: pctToDecimal(raw.maxLanceEmbutidoPct ?? group?.lance_embutido_max_pct ?? 0.25), regraPosContemplacao: (raw.regraPosContemplacao as RegraPos) || "saldo_devedor_prazo_restante", observacoesRegra: String(raw.observacoesRegra || ""), assemblyResult: assemblyRow(group) };
}

function groupNextAssembly(group?: BBGroup | null) {
  const cfg = rawConfig(group);
  const assembly = assemblyRow(group);
  const direct = group?.proxima_assembleia || group?.data_proxima_assembleia || group?.next_assembly_date || group?.nextAssemblyDate || cfg.proximaAssembleia || cfg.nextAssemblyDate || cfg.next_assembly_date || assembly.proximaAssembleia || assembly.nextAssemblyDate || assembly.next_assembly_date || assembly.proxima_assembleia || assembly.data_proxima_assembleia;
  if (direct) return String(direct);
  const text = `${group?.observacoes || ""} ${cfg.observacoesRegra || ""} ${JSON.stringify(assembly)}`;
  return text.match(/(?:pr[oó]x\.?|proxima|próxima)\s*(?:assem\.?|assembleia)?\s*[:\-]?\s*(\d{2}\/\d{2}\/\d{4})/i)?.[1] || text.match(/(\d{2}\/\d{2}\/\d{4})/)?.[1] || null;
}

function groupMedianLancePercent(group?: BBGroup | null) {
  if (!group) return 0;
  const cfg = rawConfig(group);
  const assembly = assemblyRow(group);
  const direct = pickPercent(assembly, MEDIAN_KEYS) || pickPercent(group, MEDIAN_KEYS) || pickPercent(cfg, MEDIAN_KEYS);
  if (direct) return direct;
  const bids = nestedRows(assembly).map((row) => pickPercent(row, BID_KEYS)).filter((pct) => pct > 0);
  const med = median(bids);
  if (med) return med;
  const high = pickPercent(assembly, MAX_KEYS) || pickPercent(group, MAX_KEYS) || pickPercent(cfg, MAX_KEYS);
  const low = pickPercent(assembly, MIN_KEYS) || pickPercent(group, MIN_KEYS) || pickPercent(cfg, MIN_KEYS);
  if (high && low) return (high + low) / 2;
  return high || low || 0;
}

function calcBB(input: { group: BBGroup | null; credito: number; prazo: number; parcelaContemplacao: number; lanceKey: LanceKey; lanceLivrePct: number; usarEmbutido: boolean; lanceEmbutidoPct: number }) {
  const { group, credito, prazo, parcelaContemplacao, lanceKey, lanceLivrePct, usarEmbutido, lanceEmbutidoPct } = input;
  if (!group) return { result: null as any, error: "Selecione um grupo/tabela BB." };
  if (!credito) return { result: null as any, error: "Selecione ou informe o crédito desejado." };
  const cfg = normalizeConfig(group);
  const rules = cfg.prazoRules;
  const rule = rules.find((r) => r.prazo === prazo) || rules.find((r) => r.prazo >= prazo) || rules[rules.length - 1];
  const prazoFinal = clamp(prazo || rule?.prazo || 1, Math.min(...rules.map((r) => r.prazo), rule?.prazo || 1), Math.max(...rules.map((r) => r.prazo), rule?.prazo || 1));
  const taxaAdm = Number(rule?.taxaAdmPct || 0);
  const fundoReserva = Number(rule?.fundoReservaPct || 0);
  const seguroPct = pctToDecimal(group.seguro_pct);
  const valorCategoria = credito * (1 + taxaAdm + fundoReserva);
  const parcelaBase = valorCategoria / prazoFinal;
  const seguroMensal = valorCategoria * seguroPct;
  const parcelaAntes = parcelaBase + seguroMensal;
  const lanceOption = cfg.lanceOptions.find((l) => l.key === lanceKey && l.enabled);
  if (!lanceOption) return { result: null as any, error: "A modalidade de lance selecionada não está habilitada para este grupo." };
  const lancePct = lanceKey === "livre" ? Math.max(0, lanceLivrePct) : Math.max(0, Number(lanceOption.pct || 0));
  const parcelaContempl = clamp(Math.max(1, parcelaContemplacao || 1), 1, prazoFinal);
  const prazoRestante = Math.max(1, prazoFinal - parcelaContempl);
  const lanceOfertadoValor = credito * lancePct;
  const maxEmbutidoPct = group.permite_lance_embutido === false ? 0 : Number(cfg.maxLanceEmbutidoPct || 0);
  const embPct = usarEmbutido && maxEmbutidoPct > 0 ? lanceEmbutidoPct : 0;
  if (embPct > maxEmbutidoPct) return { result: null as any, error: `Lance embutido acima do máximo permitido (${pctHuman(maxEmbutidoPct)}).` };
  const lanceEmbutidoValor = credito * embPct;
  if (lanceEmbutidoValor > lanceOfertadoValor + 0.01) return { result: null as any, error: "O lance embutido não pode ser maior que o lance ofertado." };
  const lanceProprioValor = Math.max(0, lanceOfertadoValor - lanceEmbutidoValor);
  const saldoAposPagamentos = Math.max(0, valorCategoria - parcelaBase * parcelaContempl);
  const saldoAposLance = Math.max(0, saldoAposPagamentos - lanceOfertadoValor);
  const parcelaApos = cfg.regraPosContemplacao === "mantem_parcela_reduz_prazo" ? parcelaAntes : saldoAposLance / prazoRestante + seguroMensal;
  const prazoRestanteFinal = cfg.regraPosContemplacao === "mantem_parcela_reduz_prazo" ? Math.max(1, Math.ceil(saldoAposLance / Math.max(1, parcelaAntes - seguroMensal))) : prazoRestante;
  return { result: { credito, creditoLiquido: Math.max(0, credito - lanceEmbutidoValor), prazo: prazoFinal, taxaAdm, fundoReserva, seguroPct, valorCategoria, parcelaAntes, parcelaApos, parcelaBase, seguroMensal, parcelaContemplacao: parcelaContempl, prazoRestanteAposContemplacao: prazoRestanteFinal, lancePct, lanceNome: lanceOption.nomeComercial, lanceOfertadoValor, lanceEmbutidoValor, lanceProprioValor, saldoAposLance, investimentoAteContemplacao: parcelaAntes * parcelaContempl + lanceProprioValor }, error: "" };
}

function Metric({ label, value, hint }: { label: string; value: string; hint?: string }) { return <div className="rounded-[22px] border bg-white/75 p-4 shadow-sm backdrop-blur"><div className="text-xs font-semibold uppercase tracking-wide text-slate-500">{label}</div><div className="mt-1 text-xl font-black" style={{ color: C.navy }}>{value}</div>{hint && <div className="mt-1 text-xs text-slate-500">{hint}</div>}</div>; }
function fillRoundRect(ctx: CanvasRenderingContext2D, x: number, y: number, w: number, h: number, r: number, fill: string | CanvasGradient) { ctx.beginPath(); if (ctx.roundRect) ctx.roundRect(x, y, w, h, r); else ctx.rect(x, y, w, h); ctx.fillStyle = fill; ctx.fill(); }
function wrapText(ctx: CanvasRenderingContext2D, text: string, x: number, y: number, maxWidth: number, lineHeight: number) { const words = text.split(" "); let line = ""; let cursorY = y; words.forEach((word) => { const test = line ? `${line} ${word}` : word; if (ctx.measureText(test).width > maxWidth && line) { ctx.fillText(line, x, cursorY); line = word; cursorY += lineHeight; } else line = test; }); if (line) ctx.fillText(line, x, cursorY); return cursorY + lineHeight; }
async function loadLogo() { for (const path of ["/logo.png", "/logo-consulmax.png", "/consulmax-logo.png", "/LOGO - VERS 01.png"]) { const img = new Image(); img.crossOrigin = "anonymous"; const ok = await new Promise<HTMLImageElement | null>((resolve) => { img.onload = () => resolve(img); img.onerror = () => resolve(null); img.src = path; }); if (ok) return ok; } return null; }
function drawLogo(ctx: CanvasRenderingContext2D, img: HTMLImageElement | null, x: number, y: number, maxW: number, maxH: number) { if (!img) { ctx.fillStyle = "#FFFFFF"; ctx.font = "900 64px Arial"; ctx.fillText("CONSULMAX", x, y + 70); ctx.fillStyle = C.lightGold; ctx.font = "700 24px Arial"; ctx.fillText("CONSÓRCIOS", x + 5, y + 104); return; } const ratio = Math.min(maxW / img.width, maxH / img.height); const w = img.width * ratio; const h = img.height * ratio; ctx.drawImage(img, x, y + (maxH - h) / 2, w, h); }
function drawSegmentScene(ctx: CanvasRenderingContext2D, segmento?: string | null) { const s = normalizeSegmento(segmento); fillRoundRect(ctx, 0, 1120, 1080, 800, 0, "#FFFFFF"); ctx.beginPath(); ctx.arc(540, 1160, 690, Math.PI, Math.PI * 2); ctx.fillStyle = "#FFFFFF"; ctx.fill(); ctx.strokeStyle = C.red; ctx.lineWidth = 8; ctx.stroke(); ctx.strokeStyle = C.navy; ctx.lineWidth = 14; ctx.lineCap = "round"; ctx.lineJoin = "round"; ctx.fillStyle = "rgba(30,41,63,.08)"; const oy = 1220; if (s === "imoveis") { ctx.strokeRect(210, oy + 140, 520, 260); ctx.beginPath(); ctx.moveTo(170, oy + 155); ctx.lineTo(470, oy - 70); ctx.lineTo(780, oy + 155); ctx.stroke(); ctx.fillRect(310, oy + 225, 115, 175); ctx.fillRect(535, oy + 215, 125, 92); } else if (s === "pesados") { ctx.strokeRect(120, oy + 190, 500, 170); ctx.strokeRect(620, oy + 245, 210, 115); ctx.beginPath(); [240, 560, 725].forEach((cx) => ctx.arc(cx, oy + 390, 58, 0, Math.PI * 2)); ctx.stroke(); } else if (s === "motocicletas") { ctx.beginPath(); ctx.arc(260, oy + 360, 75, 0, Math.PI * 2); ctx.arc(735, oy + 360, 75, 0, Math.PI * 2); ctx.moveTo(260, oy + 360); ctx.lineTo(430, oy + 175); ctx.lineTo(575, oy + 360); ctx.lineTo(735, oy + 360); ctx.moveTo(430, oy + 175); ctx.lineTo(640, oy + 175); ctx.lineTo(760, oy + 90); ctx.stroke(); } else { ctx.beginPath(); ctx.moveTo(120, oy + 280); ctx.lineTo(220, oy + 130); ctx.lineTo(675, oy + 130); ctx.lineTo(830, oy + 280); ctx.closePath(); ctx.stroke(); ctx.strokeRect(160, oy + 280, 630, 150); ctx.beginPath(); [280, 680].forEach((cx) => ctx.arc(cx, oy + 455, 65, 0, Math.PI * 2)); ctx.stroke(); } }
function statusRows(group: BBGroup) { const cfg = normalizeConfig(group); const prazo = [...cfg.prazoRules].sort((a, b) => b.prazo - a.prazo)[0]; const lancePct = groupMedianLancePercent(group); return cfg.creditRanges.map((range) => { const valorCategoria = range.valor * (1 + Number(prazo?.taxaAdmPct || 0) + Number(prazo?.fundoReservaPct || 0)); return { credito: brMoney(range.valor), parcela: brMoney(prazo?.prazo ? valorCategoria / prazo.prazo : 0), prazo: `${prazo?.prazo || "—"}x`, lance: pctTextFromPercent(lancePct, 1) }; }); }
async function saveStatusImage(group: BBGroup) { const rows = statusRows(group); if (!rows.length) return alert("Este grupo não possui faixas de crédito para gerar a imagem."); const canvas = document.createElement("canvas"); canvas.width = 1080; canvas.height = 1920; const ctx = canvas.getContext("2d"); if (!ctx) return; const grad = ctx.createLinearGradient(0, 0, 0, 1920); grad.addColorStop(0, "#07182F"); grad.addColorStop(0.62, C.navy); grad.addColorStop(1, "#101827"); ctx.fillStyle = grad; ctx.fillRect(0, 0, 1080, 1920); const logo = await loadLogo(); drawLogo(ctx, logo, 260, 95, 350, 125); ctx.fillStyle = "#FFFFFF"; ctx.font = "900 78px Arial"; wrapText(ctx, "Cartas de Crédito", 70, 375, 450, 86); ctx.font = "700 31px Arial"; ctx.fillText(`Próxima assembleia: ${brDate(groupNextAssembly(group))}`, 72, 610); ctx.fillStyle = C.lightGold; ctx.font = "800 34px Arial"; ctx.fillText(`Categoria: ${getStatusSegmentoLabel(group.segmento)}`, 72, 670); const tableX = 548, tableY = 245, tableW = 465, headerH = 74; const rowH = clamp(Math.floor(650 / Math.max(rows.length, 1)), 34, 58); fillRoundRect(ctx, tableX, tableY, tableW, headerH + rowH * rows.length, 20, C.red); ctx.strokeStyle = "rgba(30,41,63,.75)"; ctx.lineWidth = 3; ctx.beginPath(); ctx.moveTo(tableX + 238, tableY); ctx.lineTo(tableX + 238, tableY + headerH + rowH * rows.length); ctx.stroke(); ctx.fillStyle = "#FFFFFF"; ctx.font = "900 30px Arial"; ctx.fillText("CRÉDITO", tableX + 42, tableY + 47); ctx.font = "900 24px Arial"; ctx.fillText("PARCELA", tableX + 275, tableY + 37); ctx.font = "800 15px Arial"; ctx.fillText(rows[0]?.prazo || "PRAZO", tableX + 306, tableY + 58); rows.forEach((row, i) => { const y = tableY + headerH + rowH * i; ctx.strokeStyle = "rgba(30,41,63,.55)"; ctx.beginPath(); ctx.moveTo(tableX, y); ctx.lineTo(tableX + tableW, y); ctx.stroke(); ctx.fillStyle = "#FFFFFF"; ctx.font = `900 ${clamp(rowH * 0.43, 18, 27)}px Arial`; ctx.fillText(row.credito, tableX + 34, y + rowH * 0.68); ctx.fillText(row.parcela, tableX + 270, y + rowH * 0.68); }); ctx.fillStyle = "#FFFFFF"; ctx.font = "800 28px Arial"; ctx.fillText(`Mediana lance: ${rows[0]?.lance || "Livre"}`, tableX + 28, tableY + headerH + rowH * rows.length + 42); drawSegmentScene(ctx, group.segmento); ctx.fillStyle = "#FFFFFF"; ctx.font = "900 42px Arial"; ctx.fillText("Tire seus sonhos do papel", 70, 1050); ctx.fillText("com a Consulmax", 70, 1102); ctx.fillStyle = C.lightGold; ctx.font = "800 28px Arial"; ctx.fillText("Vagas limitadas", 72, 1148); ctx.fillStyle = C.navy; ctx.font = "900 38px Arial"; ctx.fillText(`Grupo BB Consórcios Nº ${group.grupo}`, 70, 1790); ctx.fillStyle = C.ruby; ctx.font = "800 26px Arial"; ctx.fillText("Consulmax - Maximize as suas conquistas", 70, 1834); const link = document.createElement("a"); link.download = `status-bb-consorcios-grupo-${String(group.grupo || "bb").replace(/[^a-zA-Z0-9_-]/g, "-")}.png`; link.href = canvas.toDataURL("image/png"); link.click(); }

function GrupoTabela({ group, onSelectCredit }: { group: BBGroup | null; onSelectCredit: (valor: number) => void }) { const cfg = normalizeConfig(group); if (!group || !cfg.creditRanges.length || !cfg.prazoRules.length) return null; const prazos = cfg.prazoRules; return <Card className="rounded-[28px] border bg-white/78 shadow-sm backdrop-blur"><CardHeader><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><CardTitle style={{ color: C.navy }}>Tabela do grupo selecionado</CardTitle><Button type="button" variant="secondary" className="rounded-2xl" onClick={() => saveStatusImage(group)}><Download className="mr-2 h-4 w-4" />Salvar imagem</Button></div></CardHeader><CardContent><div className="overflow-auto rounded-2xl border"><table className="w-full min-w-[820px] text-sm"><thead className="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500"><tr><th className="p-3">Faixa</th><th className="p-3">Crédito</th><th className="p-3">Prazo</th><th className="p-3">Taxa adm</th><th className="p-3">Fundo reserva</th><th className="p-3">Parcela portal</th><th className="p-3">Ação</th></tr></thead><tbody>{cfg.creditRanges.flatMap((range) => prazos.map((prazo) => { const credito = toNumber(range.valor); const parcela = prazo.prazo ? credito * (1 + prazo.taxaAdmPct + prazo.fundoReservaPct) / prazo.prazo : 0; return <tr key={`${range.id}_${prazo.id}`} className="border-t cursor-pointer transition hover:bg-slate-50" onClick={() => onSelectCredit(credito)}><td className="p-3 font-semibold text-slate-700">{range.label || brMoney(credito)}</td><td className="p-3">{brMoney(credito)}</td><td className="p-3">{prazo.prazo} meses</td><td className="p-3">{pctHuman(prazo.taxaAdmPct)}</td><td className="p-3">{pctHuman(prazo.fundoReservaPct)}</td><td className="p-3 font-semibold">{brMoney(parcela)}</td><td className="p-3"><Button type="button" variant="secondary" className="h-8 rounded-xl px-3 text-xs" onClick={(e) => { e.stopPropagation(); onSelectCredit(credito); }}>Usar crédito</Button></td></tr>; }))}</tbody></table></div><div className="mt-2 text-xs text-slate-500">Clique na linha ou no botão Usar crédito para preencher automaticamente o campo Crédito desejado.</div></CardContent></Card>; }

export default function BBConsorciosSimulator() {
  const creditoInputRef = useRef<HTMLInputElement | null>(null);
  const [loading, setLoading] = useState(true), [tableMissing, setTableMissing] = useState(false), [leads, setLeads] = useState<Lead[]>([]), [groups, setGroups] = useState<BBGroup[]>([]);
  const [userProfile, setUserProfile] = useState<LoggedUserProfile | null>(null), [bbAdminId, setBbAdminId] = useState<string | null>(null), [leadId, setLeadId] = useState(""), [segmento, setSegmento] = useState<SegmentoBB>("auto_ipca"), [groupId, setGroupId] = useState(""), [creditoInput, setCreditoInput] = useState(formatMoneyInput(0)), [prazoInput, setPrazoInput] = useState("80"), [parcelaContemplacaoInput, setParcelaContemplacaoInput] = useState("1"), [lanceKey, setLanceKey] = useState<LanceKey>("livre"), [lanceLivrePct, setLanceLivrePct] = useState("0,0000"), [usarEmbutido, setUsarEmbutido] = useState(true), [lanceEmbutidoPct, setLanceEmbutidoPct] = useState("25,0000"), [copied, setCopied] = useState(false), [salvando, setSalvando] = useState(false), [simCode, setSimCode] = useState<number | null>(null);
  async function load() { setLoading(true); setTableMissing(false); const { data: authData } = await supabase.auth.getUser(); const authUserId = authData?.user?.id ?? null; if (authUserId) { const { data: me } = await supabase.from("users").select("id,nome,phone,telefone,role,user_role").eq("auth_user_id", authUserId).maybeSingle(); setUserProfile((me as LoggedUserProfile) || null); } const [leadsReq, adminReq, groupsReq] = await Promise.all([supabase.from("leads").select("id,nome,telefone,email,owner_id").order("created_at", { ascending: false }).limit(500), supabase.from("sim_admins").select("id,name").or("name.ilike.%BB%,name.ilike.%Banco do Brasil%").limit(1), supabase.from("sim_bb_groups").select("*").order("segmento", { ascending: true }).order("grupo", { ascending: true })]); setLeads((leadsReq.data || []) as Lead[]); setBbAdminId((adminReq.data?.[0] as any)?.id ?? null); if (groupsReq.error) { if (String(groupsReq.error.message || "").toLowerCase().includes("does not exist")) setTableMissing(true); setGroups([]); } else setGroups((groupsReq.data || []) as BBGroup[]); setLoading(false); }
  useEffect(() => { load(); }, []);
  const activeGroups = useMemo(() => groups.filter((g) => g.is_active !== false), [groups]);
  const segmentGroups = useMemo(() => activeGroups.filter((g) => normalizeSegmento(g.segmento) === normalizeSegmento(segmento)), [activeGroups, segmento]);
  useEffect(() => { if (!segmentGroups.length) return setGroupId(""); if (!segmentGroups.some((g) => g.id === groupId)) setGroupId(segmentGroups[0].id); }, [segmentGroups, groupId]);
  const selectedGroup = useMemo(() => groups.find((g) => g.id === groupId) || null, [groups, groupId]);
  const selectedConfig = useMemo(() => normalizeConfig(selectedGroup), [selectedGroup]);
  const selectedLead = useMemo(() => leads.find((l) => l.id === leadId) || null, [leads, leadId]);
  const availableLances = useMemo(() => selectedConfig.lanceOptions.filter((l) => l.enabled), [selectedConfig]);
  const availablePrazos = useMemo(() => selectedConfig.prazoRules, [selectedConfig]);
  const grupoPermiteEmbutido = selectedGroup?.permite_lance_embutido !== false;
  useEffect(() => { if (availablePrazos.length && !availablePrazos.some((p) => String(p.prazo) === prazoInput)) setPrazoInput(String(availablePrazos[availablePrazos.length - 1].prazo)); if (availableLances.length && !availableLances.some((l) => l.key === lanceKey)) setLanceKey(availableLances[0].key); if (!grupoPermiteEmbutido) setUsarEmbutido(false); const livre = selectedConfig.lanceOptions.find((l) => l.key === "livre"); setLanceLivrePct(formatPercentInput(livre?.pct || 0)); setSimCode(null); }, [selectedGroup?.id, availablePrazos, availableLances, selectedConfig, prazoInput, lanceKey, grupoPermiteEmbutido]);
  function selecionarCredito(valor: number) { const credito = toNumber(valor); if (!credito) return; setCreditoInput(formatMoneyInput(credito)); setSimCode(null); requestAnimationFrame(() => { creditoInputRef.current?.focus(); creditoInputRef.current?.scrollIntoView({ behavior: "smooth", block: "center" }); }); }
  const credito = parseMoney(creditoInput), prazo = numberFrom(prazoInput), parcelaContemplacao = numberFrom(parcelaContemplacaoInput);
  const calculation = calcBB({ group: selectedGroup, credito, prazo, parcelaContemplacao, lanceKey, lanceLivrePct: parsePercent(lanceLivrePct), usarEmbutido: grupoPermiteEmbutido && usarEmbutido, lanceEmbutidoPct: parsePercent(lanceEmbutidoPct) });
  const result = calculation.result;
  const resumoTexto = useMemo(() => { if (!result || !selectedGroup) return ""; const wa = buildWhatsappLink(userProfile); const cliente = selectedLead ? `👤 Cliente: ${selectedLead.nome}\n\n` : ""; const embutido = result.lanceEmbutidoValor > 0 ? `💼 Lance embutido: ${brMoney(result.lanceEmbutidoValor)}\n` : ""; return `${cliente}🎯 *Simulação BB Consórcios ${getSegmentoLabel(selectedGroup.segmento)} - Grupo ${selectedGroup.grupo}*

💰 Crédito contratado: ${brMoney(result.credito)}
📄 Prazo contratado: ${result.prazo} meses
💳 Parcela antes da contemplação: ${brMoney(result.parcelaAntes)}

📈 Após a contemplação na parcela ${result.parcelaContemplacao}:
🏦 Lance próprio: ${brMoney(result.lanceProprioValor)}
${embutido}✅ Crédito líquido liberado: ${brMoney(result.creditoLiquido)}
💳 Parcela estimada após contemplação: ${brMoney(result.parcelaApos)}
⏳ Prazo restante: ${result.prazoRestanteAposContemplacao} meses

Me chama aqui e eu te mostro o melhor caminho 👇
${wa}`; }, [result, selectedGroup, selectedLead, userProfile]);
  async function copyResumo() { if (!resumoTexto) return; await navigator.clipboard.writeText(resumoTexto); setCopied(true); setTimeout(() => setCopied(false), 1800); }
  async function salvarSimulacao() { if (!selectedGroup || !result) return; setSalvando(true); const payload: any = { lead_id: leadId || null, lead_nome: selectedLead?.nome || null, lead_telefone: selectedLead?.telefone || null, grupo: selectedGroup.grupo || null, segmento: getSegmentoLabel(selectedGroup.segmento), nome_tabela: selectedGroup.nome_grupo || `Grupo ${selectedGroup.grupo}`, credito: result.credito, prazo_venda: result.prazo, forma_contratacao: "Parcela Cheia", seguro_prestamista: result.seguroPct > 0, lance_modelo: "percentual", lance_base: "credito", modelo_lance: result.lanceNome, lance_ofertado_pct: result.lancePct, lance_embutido_pct: result.lanceEmbutidoValor > 0 ? parsePercent(lanceEmbutidoPct) : 0, parcela_contemplacao: result.parcelaContemplacao, valor_categoria: result.valorCategoria, parcela_termo: result.parcelaBase, parcela_demais: result.parcelaAntes, lance_ofertado_valor: result.lanceOfertadoValor, lance_embutido_valor: result.lanceEmbutidoValor, lance_proprio_valor: result.lanceProprioValor, novo_credito: result.creditoLiquido, nova_parcela_sem_limite: result.parcelaApos, parcela_escolhida: result.parcelaApos, saldo_devedor_final: result.saldoAposLance, novo_prazo: result.prazoRestanteAposContemplacao, adm_tax_pct: result.taxaAdm, fr_tax_pct: result.fundoReserva }; if (bbAdminId) payload.admin_id = bbAdminId; const { data, error } = await supabase.from("sim_simulations").insert(payload).select("code").single(); setSalvando(false); if (error) return alert("Erro ao salvar simulação: " + error.message); setSimCode((data as any)?.code ?? null); }
  if (loading) return <div className="p-6 flex items-center gap-2 text-sm text-slate-600"><Loader2 className="h-5 w-5 animate-spin" /> Carregando simulador BB Consórcios...</div>;
  return <div className="p-4 md:p-6 space-y-6"><section className="relative overflow-hidden rounded-[28px] border p-6 md:p-8 shadow-sm" style={{ background: `linear-gradient(135deg, ${C.navy}, ${C.ruby})`, borderColor: "rgba(255,255,255,.22)" }}><div className="relative z-[1] flex flex-col gap-5 md:flex-row md:items-end md:justify-between"><div className="max-w-3xl text-white"><div className="mb-4 inline-flex items-center gap-2 rounded-full border border-white/20 bg-white/10 px-3 py-1 text-xs font-medium"><Calculator className="h-3.5 w-3.5" /> Simulador BB Consórcios</div><h1 className="text-2xl md:text-4xl font-black tracking-tight">Configure a estratégia de crédito e lance</h1><p className="mt-3 text-sm md:text-base text-white/82">Simulação por grupo/tabela, com dados importados automaticamente pelo robô BB.</p></div></div></section>{tableMissing && <Card className="rounded-[28px] border border-amber-200 bg-amber-50/80 shadow-sm"><CardContent className="p-5 text-sm text-amber-800"><AlertTriangle className="mr-2 inline h-5 w-5" /> Tabela sim_bb_groups ainda não existe.</CardContent></Card>}<div className="grid gap-4 xl:grid-cols-[1.05fr_.95fr]"><div className="space-y-4"><Card className="rounded-[28px] border bg-white/78 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2" style={{ color: C.navy }}><UserRound className="h-5 w-5" /> Cliente e segmento</CardTitle></CardHeader><CardContent className="grid gap-4 md:grid-cols-2"><div className="md:col-span-2"><Label>Selecionar lead</Label><select className="h-10 w-full rounded-md border px-3" value={leadId} onChange={(e) => setLeadId(e.target.value)}><option value="">Sem lead vinculado</option>{leads.map((lead) => <option key={lead.id} value={lead.id}>{lead.nome} {lead.telefone ? `• ${lead.telefone}` : lead.email ? `• ${lead.email}` : ""}</option>)}</select></div><div className="md:col-span-2 grid gap-3 md:grid-cols-6">{segmentos.map((s) => { const Icon = s.icon; const active = normalizeSegmento(segmento) === s.key; return <button key={s.key} type="button" onClick={() => setSegmento(s.key)} className="rounded-[22px] border p-4 text-left transition hover:-translate-y-0.5 hover:shadow-md" style={{ borderColor: active ? C.ruby : "rgba(30,41,63,.12)", background: active ? "rgba(161,28,39,.08)" : "white" }}><Icon className="mb-2 h-5 w-5" style={{ color: active ? C.ruby : C.navy }} /><div className="text-sm font-black" style={{ color: C.navy }}>{s.label}</div></button>; })}</div></CardContent></Card><Card className="rounded-[28px] border bg-white/78 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2" style={{ color: C.navy }}><Building2 className="h-5 w-5" /> Tabela e plano</CardTitle></CardHeader><CardContent className="grid gap-4 md:grid-cols-2"><div className="md:col-span-2"><Label>Grupo/Tabela BB</Label><select className="h-10 w-full rounded-md border px-3" value={groupId} onChange={(e) => setGroupId(e.target.value)}>{!segmentGroups.length && <option value="">Nenhum grupo cadastrado para este segmento</option>}{segmentGroups.map((g) => { const cfg = normalizeConfig(g); const min = cfg.creditRanges.length ? Math.min(...cfg.creditRanges.map((r) => r.valor)) : 0; const max = cfg.creditRanges.length ? Math.max(...cfg.creditRanges.map((r) => r.valor)) : 0; return <option key={g.id} value={g.id}>{g.nome_grupo || g.grupo} • {brMoney(min)} a {brMoney(max)}</option>; })}</select>{selectedGroup && <div className="mt-2 rounded-2xl border bg-slate-50 px-3 py-2 text-sm text-slate-700"><strong>Próxima assembleia:</strong> {brDate(groupNextAssembly(selectedGroup))} <span className="mx-2 text-slate-300">•</span> <strong>Mediana lance:</strong> {pctTextFromPercent(groupMedianLancePercent(selectedGroup), 1)}</div>}</div><div><Label>Crédito desejado</Label><Input ref={creditoInputRef} value={creditoInput} onChange={(e) => { setCreditoInput(formatMoneyInput(parseMoney(e.target.value))); setSimCode(null); }} /></div><div><Label>Prazo</Label><select className="h-10 w-full rounded-md border px-3" value={prazoInput} onChange={(e) => setPrazoInput(e.target.value)}>{availablePrazos.map((p) => <option key={p.id} value={p.prazo}>{p.prazo} meses • Adm {pctHuman(p.taxaAdmPct)} • FR {pctHuman(p.fundoReservaPct)}</option>)}</select></div><div><Label>Contemplação estimada na parcela</Label><Input value={parcelaContemplacaoInput} onChange={(e) => setParcelaContemplacaoInput(e.target.value)} /></div><div><Label>Estratégia de lance</Label><select className="h-10 w-full rounded-md border px-3" value={lanceKey} onChange={(e) => setLanceKey(e.target.value as LanceKey)}>{availableLances.map((l) => <option key={l.key} value={l.key}>{l.nomeComercial}{l.key !== "livre" ? ` • ${pctHuman(l.pct)}` : ""}</option>)}</select></div>{lanceKey === "livre" && <div><Label>Percentual do lance livre</Label><Input value={lanceLivrePct} onChange={(e) => setLanceLivrePct(e.target.value)} /></div>}<div className="rounded-2xl border bg-slate-50/70 p-3"><label className="flex items-center gap-2 text-sm font-semibold text-slate-700"><input type="checkbox" checked={grupoPermiteEmbutido && usarEmbutido} disabled={!grupoPermiteEmbutido} onChange={(e) => setUsarEmbutido(e.target.checked)} />Usar lance embutido</label><div className="mt-1 text-xs text-slate-500">Máximo do grupo: {pctHuman(selectedConfig.maxLanceEmbutidoPct)}</div></div>{grupoPermiteEmbutido && usarEmbutido && <div><Label>Percentual do lance embutido</Label><Input value={lanceEmbutidoPct} onChange={(e) => setLanceEmbutidoPct(e.target.value)} /></div>}</CardContent></Card></div><div className="space-y-4">{calculation.error && <Card className="rounded-[28px] border border-red-200 bg-red-50/80 shadow-sm"><CardContent className="flex items-center gap-2 p-4 text-sm font-semibold text-red-700"><AlertTriangle className="h-5 w-5" /> {calculation.error}</CardContent></Card>}{result && <><div className="grid gap-3 md:grid-cols-2"><Metric label="Crédito contratado" value={brMoney(result.credito)} hint={`Líquido: ${brMoney(result.creditoLiquido)}`} /><Metric label="Parcela antes" value={brMoney(result.parcelaAntes)} hint={`Categoria: ${brMoney(result.valorCategoria)} ÷ ${result.prazo}`} /><Metric label="Lance ofertado" value={brMoney(result.lanceOfertadoValor)} hint={`${result.lanceNome} • ${pctHuman(result.lancePct)}`} /><Metric label="Lance próprio" value={brMoney(result.lanceProprioValor)} hint={`Embutido: ${brMoney(result.lanceEmbutidoValor)}`} /><Metric label="Parcela pós" value={brMoney(result.parcelaApos)} hint={`${result.prazoRestanteAposContemplacao} restantes`} /><Metric label="Investimento até contemplação" value={brMoney(result.investimentoAteContemplacao)} hint={`Até parcela ${result.parcelaContemplacao}`} /></div><Card className="rounded-[28px] border bg-white/78 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2" style={{ color: C.navy }}><CheckCircle2 className="h-5 w-5" /> Resumo para WhatsApp</CardTitle></CardHeader><CardContent className="space-y-3"><textarea className="min-h-[260px] w-full rounded-2xl border bg-slate-50 p-3 text-sm" readOnly value={resumoTexto} /><div className="flex flex-wrap items-center gap-2"><Button onClick={copyResumo} className="rounded-2xl"><Copy className="mr-2 h-4 w-4" />{copied ? "Copiado!" : "Copiar resumo"}</Button><Button onClick={salvarSimulacao} disabled={salvando} className="rounded-2xl" variant="secondary">{salvando ? <Loader2 className="mr-2 h-4 w-4 animate-spin" /> : <Save className="mr-2 h-4 w-4" />}Salvar simulação</Button>{simCode && <span className="rounded-full border bg-white px-3 py-2 text-xs font-bold" style={{ color: C.ruby }}>Simulação Nº {simCode}</span>}</div></CardContent></Card></>}</div></div><GrupoTabela group={selectedGroup} onSelectCredit={selecionarCredito} /></div>;
}
